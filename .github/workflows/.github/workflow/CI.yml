name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo 'Hello World'
      - name: echo multiline string
        run: |
          this is a multiline
          string written using github
